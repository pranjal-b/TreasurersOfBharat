/* Template for supabase.config.js (committed) or optional local config.js (gitignored).
   The anon / publishable key is public by design; RLS must protect your tables. */
window.SUPABASE_URL = "https://YOUR_PROJECT_REF.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
